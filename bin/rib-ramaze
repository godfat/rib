#!/usr/bin/env ruby

require 'rib/runner'
# load rc before app to prvent your bundler (if any) kicks in
Rib.require_config
require 'rib/app/ramaze'
# load the app
Rib::Ramaze.load
Rib::Runner.run(ARGV)
